# Project-specific ProGuard/R8 rules.
#
# The release build in build.gradle.kts includes this file alongside the
# Android default optimized rules. Keep app-specific keep rules here if a
# dependency needs them later.
